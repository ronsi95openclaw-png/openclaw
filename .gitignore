.env*
__pycache__/
*.pyc
*.pyo
*.pyd
data/
.venv/
venv/
backtest-lab/.venv-backtest/
*.egg-info/
dist/
build/
.DS_Store
.claude/*
!.claude/agents/
!.claude/skills/
.superpowers/


# Archived clutter (kept on disk, out of git)
_Archive/

# Hermes / graphify outputs (rebuilt daily, never committed)
graphify-out/

# Stray binaries / artifacts that don't belong in the repo
*.exe
*.zip
*.code-search
CC-Session-Logs/

# Watchdog runtime state (per-host, never commit)
infra/watchdog_state.json

# signal_watcher.py runtime log (parked 2026-07-13, never commit its running log)
vibe-trading/signals/mnq_signals.log

# Secrets / credentials (never commit)
*.key
*.pem
*.p12
*.pfx
*.crt
*.cer
service_account*.json
