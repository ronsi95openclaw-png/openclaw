# HaulYeah's environment is now consolidated into the root .env file.
# See ../.env.example for the full template — it includes a "HAULYEAH"
# section with all keys this bot reads (TRASH_BOT_*, FB_*, GOOGLE_*,
# DRY_RUN, etc.). config.py loads ..\.env via load_dotenv(_HERE.parent / ".env").
