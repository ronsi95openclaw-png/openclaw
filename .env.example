CRYPTOCOM_API_KEY=
CRYPTOCOM_SECRET=
BLOFIN_API_KEY=
BLOFIN_SECRET=
OLLAMA_MODEL=gemma3
# Risk % of portfolio per trade — 5.0 = $5 on a $100 portfolio (min order ~$5)
TRADE_RISK_PCT=5.0

# Telegram notifications (optional — leave blank to disable)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Comma-separated list of authorized chat IDs (whitelist)
ALLOWED_CHAT_ID=

# Content pipeline
GDRIVE_WATCH_FOLDER=
MUSIC_FOLDER=content/music
WHISPER_MODEL=base

# TikTok Content Posting API
# Get from: developers.tiktok.com → your app → Content Posting API
TIKTOK_ACCESS_TOKEN=

# Instagram / Meta Graph API
# Get from: developers.facebook.com → your app → Instagram Graph API
INSTAGRAM_ACCESS_TOKEN=
INSTAGRAM_USER_ID=

# Hybrid AI Brain (Claude API)
# Get from: console.anthropic.com → API Keys
# Leave blank to use Ollama only (fully free/local)
ANTHROPIC_API_KEY=
USE_CLAUDE_API=true
MAX_TOKENS_PER_RESPONSE=500
# Word count above which a prompt is routed to Claude (default 50)
COMPLEXITY_THRESHOLD=50

# JARVIS UI Gateway (WebSocket bridge at ws://127.0.0.1:18789)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# Leave blank to disable the JARVIS Gateway on startup
GATEWAY_TOKEN=
