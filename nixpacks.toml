[phases.setup]
# System packages needed for FFmpeg (video editing) and Whisper
aptPkgs = [
  "ffmpeg",
  "libsm6",
  "libxext6",
  "libgl1",
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python start.py"
