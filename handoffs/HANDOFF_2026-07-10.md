# Handoff — 2026-07-10

**Supersedes `HANDOFF_2026-07-09.md`'s strategy framing — read this one first if both exist.**

## Session Summary
Continuation of 2026-07-09's top carry-forward item (strategy viability). What
started as "why does NQ 1H, the only profitable backtest combo, still fail the
eval" ended as a design-level finding: a real stop-cap bug in the standalone
`tjr_backtest_4yr.py` proxy engine was inflating losses 7x-81x across all four
original combos, and correcting for it doesn't rescue any of them — it reveals
the same consistency-rule failure in all four, previously masked by the bug's
own distortion. This is now understood as a property of how this TJR/kill-zone
strategy wins (low frequency, near-uniform fixed-R payouts), not which
instrument or timeframe it's pointed at. Read-only throughout — no code
changes, no commits, this session.

## Pillar State
| Pillar | Status | Last Action | Next Step |
|---|---|---|---|
| VibeTrader | Safe, paper-only, 0/25 trades | Design-level strategy finding (see above) | **Strategy-redesign conversation with Ronnie — not another read-only pass** |
| Hermes | Healthy, 22 jobs | Unchanged since 2026-07-09 | Verify vault-sync/cron prompts fired correctly (carried from 07-09, likely already resolved by now — check) |
| HaulOps | Untouched | — | — |

## Open Blockers
- 🚧 **VibeTrader, reframed:** it's no longer "NQ 1H needs a sizing fix" — no combo tested (ES 1D/1H, NQ 1D/1H) survives a corrected consistency-rule check. The strategy's win pattern (16 trades/2yr, 12 near-identical ~$635 payouts) is structurally hard to reconcile with Lucid's running-cumulative 50% rule, independent of instrument/timeframe. ES 1H is the one exception worth remembering separately: it stayed net-negative even after correction — a real, uncontaminated negative edge, not explained by the proxy bug.

## Decisions Needed Next Session
- ⚠️ **Strategy redesign path** (the real next conversation): does a materially higher-frequency or higher-win-variance version of this TJR/kill-zone design exist, or is it fundamentally incompatible with the consistency rule as specified? Three sub-options from earlier in this thread, now sharpened: (a) find higher trade frequency (more kill zones, lower timeframe, relaxed criteria), (b) fix the proxy bug and treat corrected numbers as better information before deciding, (c) let the live 25-trade paper record be the real test — paper trades run through actual `strategy.py`/`risk_guard.py`, immune to the proxy bug by construction.
- ⚠️ Memory Phase A: approve/modify/reject `GRAPHIFY_RUNTIME_MEMORY_PROPOSAL.md` (unchanged from 07-09).
- ⚠️ Git push: still 5 local commits across 2 repos awaiting explicit "yes push" (unchanged — tonight's work made none).
- Lower priority, unchanged from 07-09: `channel_directory.json` generator hunt, ClawBot-Watchdog non-zero exit.

## Exact Next Actions
| Pillar | First Command / Action |
|---|---|
| VibeTrader | No command — this is a design conversation with Ronnie first. If (b) is chosen: fix `tjr_backtest_4yr.py`'s stop cap (`stop = sweep_level ± tick` needs the same `max(stop, entry - cap)` clamp `strategy.py` uses) and re-run all four combos properly (not substitution-recomputed). |
| Hermes | Check Supabase `cron_health`: `vault-sync` last_run_at should now be non-null (fired 05:45 CT this morning); spot-check `trade-morning-brief`/`trade-eod-flatten-reminder` delivered with corrected numbers. |
| HaulOps | — |

## Context for Next Claude Instance
Don't re-litigate the proxy-bug finding — it's verified across all four combos with per-trade evidence, not just NQ 1H. Don't re-run the sizing sweep from 07-09 either — it's obsolete now that the real finding is design-level, not sizing-level. `tjr_backtest_4yr.py` is a real, unfixed tooling defect (see `MASTER_COMPACT.md`'s "Open Tooling Defects") — its numbers are not trustworthy until someone caps its stop to match `strategy.py`. The mandate file (`lucid_mandate.json`) is still canonical for all Lucid numbers, unchanged from 07-09. Nothing is pushed to git — 5 local commits await Ronnie's explicit "yes push". The strategy conversation is now sharper and more consequential than it was yesterday: it's a redesign question, not a tuning question.
