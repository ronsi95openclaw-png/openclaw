# Handoff - 2026-06-17 (end of multi-pillar build session)

## Session Summary
Folder connected; worked hands-on across all pillars. Finalized the ClawBot trade-card
->Telegram flow (manual entry), branded + extended the HaulYeah FB pipeline (it was already
compliant approve-to-send), documented the Hermes->OpenRouter switch that unblocks it
without the GPU driver, verified the dashboard, and drafted the Meta ads campaign.

## Pillar State
| Pillar     | Status     | Last Action                          | Next Step (who)                         |
|------------|------------|--------------------------------------|-----------------------------------------|
| ClawBot    | code ready | trade card + scheduler bats          | Ronnie: run infra\install_send_setups_schedule.bat |
| HaulYeah   | code ready | branding + /hired + auto-notify; tests green | Ronnie: set Sheets/FB/team-chat env, restart bot |
| Hermes     | ready-to-wire | OPENROUTER_SETUP.md written        | Ronnie: do STEP 1-4 (key+config+test)   |
| Dashboard  | healthy    | 75 tests pass                        | Ronnie: restart after Hermes is live    |
| Meta Ads   | draft      | 3-variation campaign drafted         | Ronnie: payment method + connect Page   |

## Open Blockers (Ronnie - Claude can't reach machine/secret/account)
- Hermes: put OPENROUTER_API_KEY in %LOCALAPPDATA%\hermes\.env, run the STEP 2 prompt in VS Code.
- Meta: ad account 795836823772411 has no payment method + no Page; MCP posting rollout-gated.
- HaulYeah live: GOOGLE_SHEET_ID, data/google_credentials.json, python -m agents.scraper --login,
  TRASH_BOT_TEAM_CHAT_IDS; M1 share calendar with hally-all-bot@cryptobot-trade-log.iam.gserviceaccount.com; M2 re-add @Hauly'allbot to crew group.

## Exact Next Actions
| Pillar    | Command / Action |
|-----------|------------------|
| ClawBot   | `infra\install_send_setups_schedule.bat`  (daily 07:00 trade cards) |
| HaulYeah  | set env in root .env, then `infra\restart_haulyeah.bat`; opt-in: LEAD_AUTO_NOTIFY=true, LEAD_AUTOQUEUE_MIN_SCORE=12 |
| Hermes    | follow `hermes\OPENROUTER_SETUP.md` STEP 1-4, then `infra\restart_dashboard.bat` |
| Meta Ads  | review `HaulYeah_Meta_Ads_Draft.md`; add billing + Page in Ads Manager |

## Claude can do on "yes push"
- git push the session's changes (send_setups, infra bats, trash_hauling_bot/*, hermes doc, meta draft).
  NOT pushed yet - say "yes push".

## New feature flags (default OFF)
- LEAD_AUTO_NOTIFY=true            -> HaulYeah pushes a digest of new finds to team chat after each scan
- LEAD_AUTOQUEUE_MIN_SCORE=<int>   -> auto-draft+queue approve/deny cards for leads with urgency+size >= N (try 12)

## Context for Next Claude
- Cowork Edit/Write tool corrupts files on multibyte dashes; use bash heredoc + py_compile to verify.
- HaulYeah FB pipeline is intentionally compliant (draft -> human sends). Do not build auto-DM-to-strangers.
- Trade bot must never auto-execute; cards are manual-entry only.
