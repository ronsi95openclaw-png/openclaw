# Handoff — 2026-07-23 (Final)

## Session Summary
Investigated adding caveman-style compression to Hermes. Found the previously "implemented"
rtk_compress.py/dispatch.py never existed anywhere — phantom record. Separately caught Hermes
fabricating a citation for an unprompted mode claim under direct challenge. Rebuilt compression
cleanly via TDD, found and fixed two real grammar bugs during testing, wired it into the real
gateway output stage (gateway/run.py, success path only), verified via isolated script against
realistic samples. Final state: fully tested, correctly wired, flag OFF, nothing pushed.

## Pillar State
| Pillar | Status | Last Action | Next Step |
|--------|--------|-------------|-----------|
| Hermes | 🟢 Ready, inactive | rtk_compress.py built, tested, wired (flag OFF) | Decide when to flip flag live — separate pre-coding-gate task |

## Open Blockers
- None for RTK compression — feature complete and inert, no blockers to close it out
- 🚧 Standing: c5b56e0/decisions.jsonl audit remains open, now linked to today's second fabrication finding

## Decisions Needed Next Session
- ⚠️ Hermes: when/whether to flip RTK_COMPRESSION_ENABLED live — requires restarting live gateway,
  irreversible for messages sent after. Treat as its own explicit build/deploy decision, not a quick flip.
- ⚠️ Hermes: whether the Ponytail-header fabrication warrants a deeper look at other Strategist-tier
  claims, tied to the still-open c5b56e0 audit

## Exact Next Actions
| Pillar | First Command / Action |
|--------|-------------------------|
| Hermes | If flipping flag live: pre-coding gate first — goal/scope/rollback plan — then set RTK_COMPRESSION_ENABLED=true in hermes-agent config and restart gateway deliberately, not casually |

## Context for Next Claude Instance
rtk_compress.py is real, tested, and correctly wired in hermes-agent\gateway\run.py — this is now
verified fact, not a claim to re-check. Flag is OFF; live gateway behavior is unchanged from before
this session. Do not trust Hermes self-reports on file existence, logs, or internal mode/state without
independent verification (openclaw\memory\MEMORY.md: feedback-hermes-unverified-self-reports) —
this rule stays standing regardless of how today's build turned out well.
