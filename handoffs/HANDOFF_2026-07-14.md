# Handoff — 2026-07-14

**Read `MASTER_COMPACT.md` (updated 2026-07-13) first — it's ahead of this session's
starting context and contains load-bearing facts this session initially didn't have
(NQ/ES instrument split, time-to-25-trades estimate). This handoff adds one new
finding on top of it and reconciles where this session's own earlier work landed.**

## Session Summary
Two unrelated threads this session:

1. **openclaw repo git reconciliation** (separate from vibe-trading pillar): merged
   PR #25 (3-lineage reconcile: `origin/main` + local `main`'s 52 unpushed commits +
   `hermes/auto-2026-07-07`) into `origin/main` (`fb6f4cb`), then fast-forwarded local
   `main` to match (it was fully contained, 0 ahead/55 behind — a clean FF, not a
   reset). Uncommitted wiki edits were stashed/restored across the pull, untouched.

2. **VibeTrader day-trading-frequency investigation.** Picked up the standing
   "why does the paper bot almost never trade" question. Built `eval_gate.py`
   (live-gate performance check) and loosened `strategy.py`'s FVG from
   mandatory-trigger to confirm-only (mirroring the order block) — **verified via
   git log this was already committed** (swept into `d06bffd`, the same
   auto-improve `git add -A` commit MASTER_COMPACT already flags and says was
   independently reviewed as safe on 07-13 — no lost work, no conflict). Also
   built `bot/track_record.py`, a read-only CLI report over the paper trade
   journal that calls `eval_gate.passed_evaluation()` directly so it can't drift
   out of sync with the real live-gate — also already committed in the same sweep.

   **New finding this session, not yet in `MASTER_COMPACT.md` or acted on:**
   `strategy.py:135`, `max_minutes_in_kz: int = 75` (comment: "reject entries more
   than N min into any kill zone") discards the entire back half of the NY-open
   session (mins 80-145 of a 150-min window) — confirmed via bar-count math this
   exactly explains 686 of 1,470 kill-zone-pass bars (47%) in the ES_5M.csv
   backtest. The justifying comment (`strategy.py:492-495`) cites a 3-trade sample
   ("2 of 3 stopped trades entered 125-135 min into NY open"). This is a
   plausibly-overfit tunable, unlike the kill-zone window itself or the
   sweep/OTE/MSB logic, which all check out as correct-by-design. **Not yet
   changed** — a fix (loosen to ~130-140) was proposed but the user said
   "handoff" instead of confirming before this file was written.

## Correction / Gap From This Session's Own Investigation
This session's kill-zone/late-entry backtest analysis (the bar-count numbers
above: 2,940/4,410 kill-zone fails, 686 late-entry fails, etc.) was run entirely
against `backtest/data/ES_5M.csv`. **This session did not know, until reading
`MASTER_COMPACT.md` just now, that the automated paper-trading roster switched
from ES to NQ on 2026-07-13** (`fetch_nq_data.py` added, `vibe_paper_scan.py`
repointed) after 07-13 found ES 1D/1H carry a real, uncontaminated negative edge
while NQ 1D/1H pass the corrected consistency check. The `max_minutes_in_kz`
finding is instrument-agnostic (it's in shared `strategy.py`, used by both), so
the fix recommendation still stands — but the exact bar-count evidence above is
ES-specific and should be re-verified against NQ data (once enough NQ 5-min
history exists — see the yfinance 60-day ceiling issue already logged) before
treating those specific numbers as descriptive of what's actually running live
today.

**Also surfaced late, and important given the user said "need this operating
already":** MASTER_COMPACT's 07-13 entry estimates time-to-25-trades (the
`eval_gate` sample-size floor) at **500-1,140 calendar days** at observed NQ
frequency — not weeks. If the user's urgency is about the live-gate actually
opening, `max_minutes_in_kz` alone will not get there fast; this needs to be a
number surfaced to Ronnie directly, not softened.

## Pillar State
| Pillar | Status | Last Action | Next Step |
|---|---|---|---|
| OpenClaw (openclaw repo) | 3-way lineage reconciled, PR #25 merged, local `main` synced | This session | Stray branch cleanup (`master`, old `fix/*`, orphaned `claude/*`) — deferred, no urgency |
| VibeTrader | Safe, paper-only, live roster = NQ (per 07-13), 0/25 trades toward eval_gate | `max_minutes_in_kz=75` overfit parameter identified this session, NOT yet fixed | Decide: fix `max_minutes_in_kz` now (and re-test against NQ, not stale ES set), and/or revisit the 07-10 design-level consistency-rule finding now that 07-13 narrowed it to an ES-only problem |
| Hermes | `hermes-auto-improve` disabled per MASTER_COMPACT pending 3 fixes (fork-from-main gate, scoped `git add`, real-diff commit messages) | Unchanged this session | Don't re-enable until those 3 fixes land |
| HaulOps | Untouched this session | — | — |

## Decisions Needed Next Session
- ⚠️ Apply the `max_minutes_in_kz` fix (proposed: 75 → ~130-140)? Low-risk, narrow,
  evidence-backed (unlike OTE/MSB, which should stay untouched per this session's
  own recommendation).
- ⚠️ Re-run the trade-frequency backtest against NQ data once available, not ES —
  this session's numbers are ES-only and the live bot no longer trades ES.
- ⚠️ Surface the 500-1,140-day time-to-25-trades estimate directly to Ronnie;
  decide whether the eval_gate's `MIN_SAMPLE_TRADES=25` threshold needs
  revisiting given that reality, or whether frequency fixes (like the one above)
  are expected to shrink it substantially first.
- ⚠️ Wire `track_record.py` into something Ronnie checks daily (Telegram command
  or Hermes skill) — investigated, not built; not a trivial few-line addition
  since ClawBot's Telegram bot is a separate process/repo from vibe-trading.
- Carried from 07-10/07-13, still open: Memory Phase A (`GRAPHIFY_RUNTIME_MEMORY_PROPOSAL.md`),
  `channel_directory.json` generator hunt, ClawBot-Watchdog non-zero exit,
  `lucid_mandate.json` mini/micro `max_position_size` distinction (logged 07-09).

## Exact Next Actions
| Pillar | First Command / Action |
|---|---|
| OpenClaw | None required — reconciliation is done and pushed. Branch cleanup whenever convenient. |
| VibeTrader | If fixing `max_minutes_in_kz`: edit `strategy.py:135` (75 → proposed value), re-run `bot/backtest.py` against `ES_5M.csv` to confirm trade count changes, then flag that NQ re-validation is still needed once NQ 5-min history accumulates past the yfinance 60-day ceiling. |
| Hermes | No new action — auto-improve stays disabled until its 3 fixes land (unchanged from 07-13). |
| HaulOps | — |

## Context for Next Claude Instance
`MASTER_COMPACT.md` is the authoritative ledger — read it before this file if
both exist, since it's dated later (07-13) than this handoff's own prior context
(07-10). This session's vibe-trading contributions (`eval_gate.py`,
`track_record.py`, the FVG confirm-only change) are already safely committed —
verified via `git log`/`git status`, not re-applied, no duplicate work. The one
genuinely new, unintegrated item from this session is the `max_minutes_in_kz=75`
overfit-parameter finding — treat that as the actionable carry-forward, not the
already-committed eval_gate/FVG/track_record work. Don't re-run the kill-zone/
late-entry bar-count investigation from scratch; it's fully evidenced above with
exact file/line references. Do treat the ES-vs-NQ data mismatch as a real gap:
this session's specific bar counts describe ES only, and ES is no longer the
live-traded instrument as of 07-13.
